package seraph.zion.signal.payments.preferences.viewholder;

import android.view.View;

import androidx.annotation.NonNull;

import seraph.zion.signal.payments.preferences.model.InProgress;
import seraph.zion.signal.util.adapter.mapping.MappingViewHolder;

public class InProgressViewHolder extends MappingViewHolder<InProgress> {

  public InProgressViewHolder(@NonNull View itemView) {
    super(itemView);
  }

  @Override
  public void bind(@NonNull InProgress model) { }
}
